import { UserActions, UserActionTypes } from '../../../types/types';
import { Dispatch } from "redux";



export const GetUserList = () => async (dispatch: Dispatch<UserActions>) => {
	try {

	} catch (error) {

	}
}

